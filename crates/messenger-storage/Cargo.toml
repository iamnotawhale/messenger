[package]
name = "messenger-storage"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1.89"
messenger-protocol = { version = "0.1.0", path = "../messenger-protocol" }
thiserror = "2.0.18"
